#include <stdio.h>

double GetMinutesAsHours(double origMinutes) {

   double hours;
   hours = origMinutes / 60;
   return hours;

}

int main(void) {
   double minutes;
   scanf("%lf", &minutes);
   printf("%f\n", GetMinutesAsHours(minutes));

   return 0;
}
