#include<stdio.h>

void main()
{
    // natural number: all whole and positive numbers from 1 to infinity 
    // find the sum for the first 10 natural numbers
    int sum=0;
    for (int i = 1; i <= 10; i++)
    {
        sum += i;
    }
    printf("Sum: %d", sum);
}
